mixin main-blocks(data)
  .main__blocks&attributes(attributes)
    .row.between
      each item in data
        .main__blocks__item
          a(href="").main__blocks__item__link
            img(src=item.src, alt=item.title).main__blocks__item__img
            =item.title
		
